{
  "name": "@robotania/agent-sdk",
  "version": "1.2.1",
  "description": "Robotania Agent SDK — precise TypeScript and CLI tools for AI agent integration",
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "docs",
    "README.md"
  ],
  "bin": {
    "robotania-init": "dist/bin/init.js",
    "robotania": "dist/bin/robotania.js",
    "robotania-bridge": "dist/bin/robotania-bridge.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "dev": "tsc --watch",
    "pretest": "pnpm build",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:bundle": "node scripts/bundle.mjs",
    "build:bundle:bridge": "node scripts/bundle-bridge.mjs",
    "build:binary": "node scripts/build-binary.mjs",
    "build:binary:bridge": "node scripts/build-bridge-binary.mjs",
    "build:kit": "node scripts/build-kit.mjs",
    "build:bridge-kit": "node scripts/build-bridge-kit.mjs",
    "build:docs-kit": "node scripts/build-docs-kit.mjs",
    "pack:release": "node scripts/pack-release.mjs",
    "build:release:linux": "node scripts/build-release.mjs --target linux-x64",
    "build:release:win": "node scripts/build-release.mjs --target win-x64",
    "build:release:mac": "node scripts/build-release.mjs --target macos-arm64",
    "build:release:native": "node scripts/build-release.mjs --target native",
    "build:all:linux": "pnpm build:release:linux",
    "build:all:win": "pnpm build:release:win",
    "build:all:mac": "pnpm build:release:mac",
    "build:all": "pnpm build:release:native",
    "release:pack": "pnpm pack:release",
    "release:all:linux": "pnpm test && pnpm build:release:linux",
    "release:all:win": "pnpm test && pnpm build:release:win",
    "release:all:mac": "pnpm test && pnpm build:release:mac",
    "release:native": "pnpm test && pnpm build:release:native",
    "release:all": "pnpm release:native"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "viem": "^2.21.0",
    "ws": "^8.18.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "@types/ws": "^8.5.14",
    "@yao-pkg/pkg": "^5.12.0",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.0",
    "typescript": "^5.4.0",
    "vitest": "^3.0.0"
  },
  "keywords": [
    "robotania",
    "agent",
    "sdk",
    "ai",
    "spectator",
    "positions",
    "arena"
  ],
  "license": "MIT"
}
